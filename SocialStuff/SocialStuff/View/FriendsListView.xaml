<Window
    x:Class="SocialStuff.View.FriendsListView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:SocialStuff.View"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:model="using:SocialStuff.Model"
    xmlns:viewmodel="using:SocialStuff.ViewModel"
    mc:Ignorable="d">

    <Grid Padding="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>

        <!-- Title and Add Button -->
        <TextBlock Grid.Row="0" Grid.Column="0" Text="Friends" FontSize="24" VerticalAlignment="Center"/>
        <Button Grid.Row="0" Grid.Column="1" Content="+" FontSize="24" VerticalAlignment="Center" Click="AddFriendButton_Click"/>

        <!-- Search Bar -->
        <TextBox Grid.Row="1" Grid.ColumnSpan="2" x:Name="SearchBox" PlaceholderText="Search friends..." 
          Text="{x:Bind ViewModel.SearchQuery, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Margin="0,10,0,10"/>


        <!-- Friends List -->
        <ListView Grid.Row="2" Grid.ColumnSpan="2" x:Name="FriendsListBox"
           ItemsSource="{x:Bind ViewModel.Friends, Mode=OneWay}"
           SelectedItem="{x:Bind ViewModel.SelectedFriend, Mode=TwoWay}"
           Margin="0,10,0,0">

            <ListView.ItemTemplate>
                <DataTemplate x:DataType="model:User">
                    <Grid Padding="10">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <StackPanel Grid.Column="0" Orientation="Vertical">
                            <TextBlock Text="{x:Bind GetUsername()}" FontWeight="SemiBold"/>
                            <TextBlock Text="{x:Bind GetPhoneNumber()}" Opacity="0.7" FontSize="12"/>
                        </StackPanel>

                        <Button Grid.Column="1" 
                            Content="Remove" 
                            Click="RemoveFriendButtonClick"
                            CommandParameter="{x:Bind}"
                            Margin="10,0,0,0"/>     
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>

        <!-- No Friends Text -->
        <TextBlock x:Name="NoFriendsTextBlock"
           Grid.Row="2" Grid.ColumnSpan="2"
           Text="You have no friends in the friends list, add a friend to view it here. :)"
           HorizontalAlignment="Center" 
           VerticalAlignment="Center"
           Margin="0,20,0,0"
           Visibility="Collapsed"/>
    </Grid>
</Window>
